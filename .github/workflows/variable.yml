name: variable

on:
  workflow_dispatch
env: 
 MY_VAR: workflow

jobs:
  variables:
   env:
    MY_VAR: jobs
   runs-on: ubuntu_latest
   steps:
     - name: Print
       run: echo '${{env.MY_VAR}}'
       env:
         MY_VAR: steps
    
  
