name: PR Event Details Workflow

on:
  pull_request:
    types:
      - opened
      - edited
      - labeled
      - unlabeled
      - assigned
      - unassigned
      - synchronize

jobs:
  show-pr-details:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Display PR Event Metadata
        run: |
          echo "🔔 GitHub Event Name: ${{ github.event_name }}"
          echo "⚡ PR Action Type: ${{ github.event.action }}"
          echo "👤 PR Author: ${{ github.event.pull_request.user.login }}"
          echo "📅 PR Created At: ${{ github.event.pull_request.created_at }}"
